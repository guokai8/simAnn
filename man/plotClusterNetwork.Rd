% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{plotClusterNetwork}
\alias{plotClusterNetwork}
\title{Plot Network Visualization of Term Clusters}
\usage{
plotClusterNetwork(
  cluster_result,
  vertex_size = 10,
  show_names = FALSE,
  layout = c("fr", "circle"),
  custom_colors = NULL,
  parent_shape = "square",
  child_shape = "circle",
  relation_types = NULL,
  label_size = 0.7,
  label_dist = 1.5,
  repel_force = 1,
  node_alpha = 0.8,
  show_legend = FALSE,
  ...,
  plot_genes = FALSE,
  top_n_genes = 5,
  selected_genes = NULL
)
}
\arguments{
\item{cluster_result}{A cluster result object from clusterST containing:
\itemize{
  \item clusters: List of term clusters
  \item graph: igraph object with cluster structure
  \item membership: Cluster assignments
}}

\item{vertex_size}{Numeric value for base node size in the plot.
Larger values create bigger nodes. Default: 10.}

\item{show_names}{Logical indicating whether to display term names instead of
GO IDs in node labels. Default: FALSE.}

\item{layout}{Character string specifying the layout algorithm:
\describe{
  \item{fr}{Fruchterman-Reingold force-directed layout}
  \item{circle}{Circular layout with nodes arranged in a circle}
}}

\item{custom_colors}{Character vector of colors for clusters. If provided,
these colors will be used instead of the default palette.}

\item{parent_shape}{Character string specifying the shape for parent nodes
(e.g., "square", "circle", "rectangle", "csquare", "crectangle").}

\item{child_shape}{Character string specifying the shape for child nodes.}

\item{relation_types}{Character vector specifying which types of relations to display.
       NULL shows all relation types. Options include:
\itemize{
  \item "is_a"
  \item "part_of"
  \item "regulates"
  \item "parent-child"
}}

\item{label_size}{Numeric multiplier for node label size. Default: 0.7.}

\item{label_dist}{Numeric value for distance of labels from nodes. Default: 1.5.}

\item{repel_force}{Numeric value for force of node repulsion in layout. Default: 1.}

\item{node_alpha}{Numeric value between 0 and 1 for node color transparency.
Default: 0.8.}

\item{show_legend}{Logical indicating whether to display the plot legend.
Default: TRUE.}

\item{...}{Additional parameters passed to plot.igraph for fine-tuning the
visualization.}
}
\value{
Invisibly returns the igraph object used for plotting, which can be
        used for further customization or analysis.

a igraph figure
}
\description{
Creates an interactive network visualization of term clusters showing hierarchical
relationships. The visualization displays parent-child relationships between terms
using different line styles for relation types and colors for cluster membership.
The function provides extensive customization options for node appearance, layout,
and labeling.
}
\details{
The network visualization represents:
\itemize{
  \item Nodes: GO terms (parents and children)
  \item Edges: Relationships between terms
  \item Colors: Different clusters
  \item Shapes: Distinguish between parent and child terms
  \item Line styles: Different types of relationships
}
}
\examples{
\dontrun{
# Load required packages and build GO tree
library(org.Hs.eg.db)
tree <- buildGOTree(species = "human", namespace = "BP")

# Generate clusters using Wang's method
weights <- c("is_a" = 0.8, "part_of" = 0.6)
clusters <- clusterST(tree,
                     terms = 1:30,
                     method = "wang",
                     weights = weights,
                     threshold = 0.2)

# Basic plot with term names
plotClusterNetwork(clusters,
                  show_names = TRUE)

# Customized visualization
plotClusterNetwork(clusters,
   show_names = TRUE,
   layout = "fr",
   vertex_size = 12,
   label_size = 0.6,
   label_dist = 2,
   repel_force = 1.2,
   node_alpha = 0.85,
   parent_shape = "square",
   child_shape = "circle")

# Use custom color palette
nature_colors <- c("#2166AC", "#4393C3", "#92C5DE", "#D6604D")
plotClusterNetwork(clusters,
   relation_types = c("is_a", "part_of"),
   custom_colors = nature_colors,
   show_names = TRUE,
   show_legend = TRUE)

# Focus on specific relation types
plotClusterNetwork(clusters,
   relation_types = "is_a",
   vertex_size = 15,
   show_names = TRUE,
   layout = "circle")
}

}
\references{
Csardi G, Nepusz T (2006). The igraph software package for complex network
research. InterJournal, Complex Systems, 1695.
}
\seealso{
\code{\link{clusterST}} for generating term clusters
\code{\link[igraph]{plot.igraph}} for additional plotting parameters
}
\author{
Kai Guo
}
